{
  "name": "test-vue",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/test-vue/src",
  "// targets": "to see all targets run: nx show project test-vue --web",
  "targets": {
    "targets": {
      "serve": {
        "executor": "nx:run-commands",
        "options": {
          "cwd": "./apps/test-vue",
          "commands": [
            {
              "command": "vite serve"
            }
          ]
        }
      }
    }
  }
}
