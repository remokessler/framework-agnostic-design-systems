# ui-core-elements-pages

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ui-core-elements-pages` to build the library.

## Running unit tests

Run `nx test ui-core-elements-pages` to execute the unit tests via [Jest](https://jestjs.io).
